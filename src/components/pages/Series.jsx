/** @jsx React.DOM */
var React = require('react');

var Series = React.createClass({

    render: function () {
        return (
            <div>
                <h1>Series</h1>
            </div>
        );
    }
});

module.exports = Series;
